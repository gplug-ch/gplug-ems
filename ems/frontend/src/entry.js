/* Vite entry point. Importing the stylesheet here lets Vite emit a hashed CSS
   asset and inject its <link> into index.html; main.js runs the boot sequence. */
import '../style.css';
import './main.js';
